import { formatIconSize as a } from "@primeicons/core/utils";
import { toKebabCase as u } from "@primeuix/utils/object";
import { defineComponent as p, computed as c, h as l } from "vue";
const d = ([t, o]) => {
  const { key: i, ...e } = o, n = {};
  for (const [r, s] of Object.entries(e))
    n[u(r)] = s;
  return l(t, { key: i, ...n });
}, b = (t) => {
  const o = {
    size: {
      type: [Number, String],
      default: void 0
    },
    color: {
      type: String,
      default: void 0
    },
    spin: {
      type: Boolean,
      default: !1
    }
  };
  return { Icon: p({
    name: t.name.split("-").map((e) => e.charAt(0).toUpperCase() + e.slice(1)).join(""),
    props: o,
    setup(e, { attrs: n }) {
      const r = c(() => e.size ?? 20), s = c(() => ({
        ...a(e.size) && { "--px-icon-size": a(e.size) },
        ...e.color && { color: e.color }
      })), m = c(() => ["p-icon", `p-icon-${t.name}`, e.spin && "p-icon-spin"].filter(Boolean));
      return () => l(
        "svg",
        {
          ...t.svg,
          width: r.value,
          height: r.value,
          "aria-hidden": "true",
          ...n,
          style: s.value,
          class: m.value
        },
        t.nodes.map(d)
      );
    }
  }), props: o };
};
export {
  b as createIcon
};
